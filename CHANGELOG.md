# JadeDbClient Changelog

## [1.5.11] - 2026-03-03

### Added
- **`JadeDbColumn.IsIdentity`**: New property on `[JadeDbColumn]` attribute. Set `IsIdentity = true` on the primary-key / identity column so that `BuildInsert(returnIdentity: true)` returns the correct column name in the database-specific clause (`RETURNING <col>` for PostgreSQL, `OUTPUT INSERTED.<col>` for SQL Server, `SELECT LAST_INSERT_ID()` for MySQL). If no property carries `IsIdentity = true`, the builder falls back to the conventional column name `id`.

### Changed
- **Version bump** from 1.5.10 to 1.5.11.

---

## [1.15.10] - 2026-03-03

### Changed
- **Renamed `IsAutoIncrement` → `IgnoreOnInsert`**: The `JadeDbColumnAttribute` property has been renamed from `IsAutoIncrement` to `IgnoreOnInsert` for clarity. The intent is broader than auto-increment — it covers any column the database manages (IDENTITY, computed defaults, rowversion, etc.).
  - Migration: replace `[JadeDbColumn(IsAutoIncrement = true)]` with `[JadeDbColumn(IgnoreOnInsert = true)]`.
- **Version bump** from 1.5.9 to 1.15.10.

---

## [1.5.9] - 2026-03-03

### Added
- **QueryBuilder.CountAsync()**: Support for executing `SELECT COUNT(*)` queries asynchronously while respecting fluent `Where` filters and `Join` clauses.
- **Explicit column exclusion from writes**: Properties can now be marked with `[JadeDbColumn(IsAutoIncrement = true)]` *(renamed to `IgnoreOnInsert` in v1.15.10)* to be automatically excluded from `INSERT` and `UPDATE` statements generated by `QueryBuilder`.
- **Parameterless constructor on `JadeDbColumnAttribute`**: Allows marking a column for exclusion from writes without renaming it.

### Changed
- **Insert/Update Behavior**: `QueryBuilder` no longer uses a name-based convention (like "Id") to skip columns. Relies strictly on the explicit attribute flag, ensuring tables without database-generated keys work without extra configuration.
- **Source Generator Update**: The AOT-safe source generator now respects the attribute flag and excludes marked properties from bulk insert accessors.
